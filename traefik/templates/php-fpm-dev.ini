; Development overrides for the PHP-FPM container.
; Keep upload_max_filesize in sync with client_max_body_size in nginx.conf.
upload_max_filesize = 128M
post_max_size = 128M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
max_input_vars = 5000

; Pick up edits from the bind mount on the next request.
opcache.revalidate_freq = 0
opcache.validate_timestamps = 1
